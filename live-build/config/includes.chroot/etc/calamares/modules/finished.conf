---
# Keep the familiar opt-out restart choice while using Calamares' current
# schema. The installer never forces an immediate reboot.
restartNowMode: user-checked
restartNowCommand: "systemctl -i reboot"
notifyOnFinished: false
