Source: shadowfetch-control-center
Section: x11
Priority: optional
Maintainer: Shadowfetch Project <signing@shadowfetch.com>
Build-Depends: debhelper-compat (= 13), dh-python, python3
Standards-Version: 4.6.2
Homepage: https://www.shadowfetchlinux.org/

Package: shadowfetch-control-center
Architecture: all
Depends:
 ${misc:Depends},
 python3,
 python3-pyqt6,
 python3-dbus,
 shadowfetch-defaults (= ${binary:Version}),
 shadowfetch-missions (= ${binary:Version})
Recommends:
 shadowfetch-ember,
# software_page.py imports sfcc.fireproof_page, a module shadowfetch-fireproof
# drops into this package's own sfcc directory. The import is guarded and falls
# back to an honest card, so this is Recommends and not Depends -- but it was
# not declared at all, in either direction, and Software & Updates without it
# is a page that cannot update anything.
 shadowfetch-fireproof,
 shadowfetch-firewatchd,
# busutil.py runs /usr/libexec/shadowfetch-hwscan and reads the fact file it
# writes; the Agents and Drivers pages are built out of it. Undeclared until
# now, so a Control Center installed without it showed "unknown" hardware with
# nothing in the graph explaining why.
 shadowfetch-hwscan,
 shadowfetch-phoenix,
 shadowfetch-welcome,
 snapper,
 gamemode,
 polkitd,
 pkexec,
 konsole
Replaces: shadowfetch-defaults (<< 2.1.2-1~)
Breaks: shadowfetch-defaults (<< 2.1.2-1~)
Description: Shadowfetch Mission Control - agent tasks and system care
 The native desktop starts with persistent missions and the featured official
 Grok Bot integration. It also includes the private System Passport, Element
 Workbench, Ignite, Watch, Recover, Workspaces, Drivers and Software & Updates.
 Supports deep links
 (--page section[:tab]) with single-instance routing over the session
 bus, so Welcome, Firewatch, Phoenix and Fireproof all land in the same
 window.
 .
 Takes over /usr/bin/shadowfetch-control from shadowfetch-defaults; the
 2.1.1 tools (Safe Update, System Health, Agent Studio, Agent Tools,
 First-run Setup, Graphics & Recovery) remain reachable inside their
 sections. All privileged actions go through systemd/polkit or pkexec
 helpers shipped by the pillar packages; the UI owns no root logic and
 makes no network connections.
