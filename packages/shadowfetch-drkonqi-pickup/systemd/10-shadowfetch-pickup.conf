# Keep upstream ordering, socket activation and RuntimeMaxSec safety limit.
# The separate helper completes the finite login pickup scan successfully.
[Service]
ExecStart=
ExecStart=/usr/libexec/shadowfetch-drkonqi-pickup --settle-first --pickup --uid %U
