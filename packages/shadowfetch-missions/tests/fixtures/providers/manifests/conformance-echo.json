{
  "schema_version": 1,
  "id": "conformance-echo",
  "display_name": "Conformance echo (test fixture)",
  "interface_version": 1,
  "adapter_module": "sf_provider_conformance_echo",
  "adapter_class": "ConformanceEchoProvider",
  "capabilities": [
    "sourced_report"
  ],
  "credential_ids": [
    "CONFORMANCE_ECHO_TOKEN"
  ],
  "network_policy": "allowlist",
  "egress_allowlist": [
    "conformance.example.com"
  ],
  "sandbox_profile": {
    "workspace_mode": "read-only",
    "memory_mb": 512,
    "cpu_seconds": 60,
    "processes": 8,
    "read_grants": [
      "/usr/share/doc"
    ],
    "masked_paths": [
      "/home"
    ]
  },
  "executable": {
    "kind": "absolute",
    "path": "/usr/bin/true"
  },
  "package": "shadowfetch-missions",
  "version": "4.0.0",
  "notes": "Conformance suite fixture. A deliberately VALID third provider that does not ship: it exists to prove a new provider is reachable from its manifest alone, with no edit to the orchestrator, the CLI, the desktop UI or the release gate. Read-only workspace, small limits, one egress host, one credential identity, and a plain-text native stream that is neither JSONL nor ffmpeg output."
}
