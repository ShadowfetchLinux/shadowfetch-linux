#!/usr/bin/env python3
"""Installed and source-tree entry point for Mission Control."""
import sys
from pathlib import Path
sys.path.insert(0, "/usr/lib/shadowfetch/missions")
for parent in Path(__file__).resolve().parents:
    candidate = parent / "data/usr/lib/shadowfetch/missions"
    if (candidate / "sf_missions.py").is_file():
        sys.path.insert(0, str(candidate))
        break
from sf_missions import main
raise SystemExit(main())
